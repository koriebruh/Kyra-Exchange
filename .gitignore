# IDE
.idea/
*.iml
.vscode/

# Claude Code local
.claude/settings.local.json

# Build
target/
build/
*.class

# Env & secrets — JANGAN pernah commit
.env
.env.*
*.pem
*.key
secrets/
!*.sops.*
# Exception: DEV-ONLY JWT keys for local/test. Prod keys come from secrets,
# are injected via env/SOPS, and are NEVER committed.
!**/resources/jwt/*.pem
!**/resources/**/jwt/*.pem

# Logs & temp
*.log
.DS_Store

# Quarkus CLI artifacts
.quarkus/

# jqwik
.jqwik-database
